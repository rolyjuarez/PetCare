<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.servicio.mapper.ServicioMapper">
    <sql id="baseColumns">
        id, nombre, descripcion, duracion_minutos, precio_base, imagen_url, activo, categoria,
        created_at, updated_at, deleted, version
    </sql>
    <sql id="table">petcare.servicio</sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="bo.capital.tec.pet.servicio.entity.Servicio">
        INSERT INTO <include refid="table"/> (nombre, descripcion, duracion_minutos, precio_base, imagen_url, activo, categoria)
        VALUES (#{nombre}, #{descripcion}, #{duracionMinutos}, #{precioBase}, #{imagenUrl}, #{activo}, #{categoria})
    </insert>

    <select id="selectById" resultType="bo.capital.tec.pet.servicio.entity.Servicio">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE id = #{id} AND deleted = FALSE
    </select>

    <select id="selectAll" resultType="bo.capital.tec.pet.servicio.entity.Servicio">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        <where>
            deleted = FALSE
            <if test="nombre != null and nombre != ''">AND nombre ILIKE '%' || #{nombre} '%'</if>
        </where>
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        <where>
            deleted = FALSE
            <if test="nombre != null and nombre != ''">AND nombre ILIKE '%' || #{nombre} '%'</if>
        </where>
    </select>

    <update id="update" parameterType="bo.capital.tec.pet.servicio.entity.Servicio">
        UPDATE <include refid="table"/>
        SET nombre = #{nombre}, descripcion = #{descripcion}, duracion_minutos = #{duracionMinutos},
            precio_base = #{precioBase}, imagen_url = #{imagenUrl}, activo = #{activo}, categoria = #{categoria},
            updated_at = NOW(), version = version + 1
        WHERE id = #{id} AND deleted = FALSE
    </update>

    <update id="softDelete">
        UPDATE <include refid="table"/> SET deleted = TRUE, updated_at = NOW(), version = version + 1 WHERE id = #{id}
    </update>

    <select id="selectActive" resultType="bo.capital.tec.pet.servicio.entity.Servicio">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE activo = TRUE AND deleted = FALSE
        ORDER BY nombre ASC
    </select>
</mapper>
