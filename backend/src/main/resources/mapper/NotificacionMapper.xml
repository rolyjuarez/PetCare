<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.notificacion.mapper.NotificacionMapper">
    <sql id="baseColumns">
        id, usuario_id, titulo, mensaje, tipo, leida, fecha_lectura,
        created_at, updated_at, deleted, version
    </sql>
    <sql id="table">petcare.notificacion</sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="bo.capital.tec.pet.notificacion.entity.Notificacion">
        INSERT INTO <include refid="table"/> (titulo, mensaje, tipo, leida, usuario_id)
        VALUES (#{titulo}, #{mensaje}, #{tipo}, #{leida}, #{usuarioId})
    </insert>

    <select id="selectById" resultType="bo.capital.tec.pet.notificacion.entity.Notificacion">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE id = #{id} AND deleted = FALSE
    </select>

    <select id="selectAll" resultType="bo.capital.tec.pet.notificacion.entity.Notificacion">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE deleted = FALSE
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        WHERE deleted = FALSE
    </select>

    <select id="selectByUsuarioId" resultType="bo.capital.tec.pet.notificacion.entity.Notificacion">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE usuario_id = #{usuarioId} AND deleted = FALSE
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUsuarioIdAndLeida" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        WHERE usuario_id = #{usuarioId} AND leida = #{leida} AND deleted = FALSE
    </select>

    <update id="updateLeida">
        UPDATE <include refid="table"/>
        SET leida = TRUE, updated_at = NOW(), version = version + 1
        WHERE id = #{id} AND deleted = FALSE
    </update>

    <delete id="deleteOld">
        DELETE FROM <include refid="table"/>
        WHERE usuario_id = #{usuarioId}
          AND created_at &lt; NOW() - INTERVAL '30 days'
    </delete>
</mapper>
