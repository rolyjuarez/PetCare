<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.modules.menu.mapper.PermisoMapper">
    <sql id="baseColumns">
        p.id, p.nombre, p.descripcion, p.rol_id, p.menu_id, p.submenu_id,
        p.crear, p.leer, p.actualizar, p.eliminar,
        p.created_at, p.updated_at, p.deleted, p.version
    </sql>
    <sql id="table">permiso p</sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="bo.capital.tec.pet.modules.menu.entity.Permiso">
        INSERT INTO permiso (nombre, descripcion, rol_id, menu_id, submenu_id,
            crear, leer, actualizar, eliminar)
        VALUES (#{nombre}, #{descripcion}, #{rolId}, #{menuId}, #{submenuId},
            #{crear}, #{leer}, #{actualizar}, #{eliminar})
    </insert>

    <select id="selectById" resultType="bo.capital.tec.pet.modules.menu.entity.Permiso">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE p.id = #{id} AND p.deleted = FALSE
    </select>

    <select id="selectByRolId" resultType="bo.capital.tec.pet.modules.menu.entity.Permiso">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE p.rol_id = #{rolId} AND p.deleted = FALSE
    </select>

    <select id="selectAll" resultType="bo.capital.tec.pet.modules.menu.entity.Permiso">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        <where>
            p.deleted = FALSE
            <if test="nombre != null and nombre != ''">AND p.nombre ILIKE '%' || #{nombre} '%'</if>
        </where>
        ORDER BY p.created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        <where>
            p.deleted = FALSE
            <if test="nombre != null and nombre != ''">AND p.nombre ILIKE '%' || #{nombre} '%'</if>
        </where>
    </select>

    <update id="update" parameterType="bo.capital.tec.pet.modules.menu.entity.Permiso">
        UPDATE permiso
        SET nombre = #{nombre}, descripcion = #{descripcion}, rol_id = #{rolId},
            menu_id = #{menuId}, submenu_id = #{submenuId},
            crear = #{crear}, leer = #{leer}, actualizar = #{actualizar}, eliminar = #{eliminar},
            updated_at = NOW(), version = version + 1
        WHERE id = #{id} AND deleted = FALSE
    </update>

    <update id="softDelete">
        UPDATE permiso SET deleted = TRUE, updated_at = NOW(), version = version + 1 WHERE id = #{id}
    </update>
</mapper>
