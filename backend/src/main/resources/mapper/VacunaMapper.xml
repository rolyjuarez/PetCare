<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.vacuna.mapper.VacunaMapper">
    <sql id="baseColumns">
        id, nombre, descripcion, periodicidad_meses, created_at, updated_at, deleted, version
    </sql>
    <sql id="table">vacuna</sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="bo.capital.tec.pet.vacuna.entity.Vacuna">
        INSERT INTO <include refid="table"/> (nombre, descripcion, periodicidad_meses)
        VALUES (#{nombre}, #{descripcion}, #{periodicidadMeses})
    </insert>

    <select id="selectById" resultType="bo.capital.tec.pet.vacuna.entity.Vacuna">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE id = #{id} AND deleted = FALSE
    </select>

    <select id="selectAll" resultType="bo.capital.tec.pet.vacuna.entity.Vacuna">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE deleted = FALSE
        ORDER BY nombre ASC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        WHERE deleted = FALSE
    </select>

    <update id="update" parameterType="bo.capital.tec.pet.vacuna.entity.Vacuna">
        UPDATE <include refid="table"/>
        SET nombre = #{nombre}, descripcion = #{descripcion}, periodicidad_meses = #{periodicidadMeses},
            updated_at = NOW(), version = version + 1
        WHERE id = #{id} AND deleted = FALSE
    </update>

    <update id="softDelete">
        UPDATE <include refid="table"/> SET deleted = TRUE, updated_at = NOW(), version = version + 1 WHERE id = #{id}
    </update>
</mapper>
