<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.promocion.mapper.PromocionMapper">
    <sql id="baseColumns">
        id, codigo, nombre, descripcion, tipo_descuento, valor_descuento,
        fecha_inicio, fecha_fin, activa, limite_usos, usos_actuales,
        created_at, updated_at, deleted, version
    </sql>
    <sql id="table">petcare.promocion</sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="bo.capital.tec.pet.promocion.entity.Promocion">
        INSERT INTO <include refid="table"/> (codigo, nombre, descripcion, tipo_descuento, valor_descuento,
            fecha_inicio, fecha_fin, activa, limite_usos, usos_actuales)
        VALUES (#{codigo}, #{nombre}, #{descripcion}, #{tipoDescuento}, #{valorDescuento},
            #{fechaInicio}, #{fechaFin}, #{activa}, #{limiteUsos}, #{usosActuales})
    </insert>

    <select id="selectById" resultType="bo.capital.tec.pet.promocion.entity.Promocion">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE id = #{id} AND deleted = FALSE
    </select>

    <select id="selectByCodigo" resultType="bo.capital.tec.pet.promocion.entity.Promocion">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE codigo = #{codigo} AND deleted = FALSE
    </select>

    <select id="selectAll" resultType="bo.capital.tec.pet.promocion.entity.Promocion">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE deleted = FALSE
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectActive" resultType="bo.capital.tec.pet.promocion.entity.Promocion">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE activa = TRUE
          AND fecha_inicio &lt;= CURRENT_DATE
          AND fecha_fin >= CURRENT_DATE
          AND deleted = FALSE
        ORDER BY codigo ASC
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        WHERE deleted = FALSE
    </select>

    <select id="countActive" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        WHERE activa = TRUE
          AND fecha_inicio &lt;= CURRENT_DATE
          AND fecha_fin >= CURRENT_DATE
          AND deleted = FALSE
    </select>

    <update id="update" parameterType="bo.capital.tec.pet.promocion.entity.Promocion">
        UPDATE <include refid="table"/>
        SET codigo = #{codigo}, nombre = #{nombre}, descripcion = #{descripcion},
            tipo_descuento = #{tipoDescuento}, valor_descuento = #{valorDescuento},
            fecha_inicio = #{fechaInicio}, fecha_fin = #{fechaFin},
            activa = #{activa}, limite_usos = #{limiteUsos}, usos_actuales = #{usosActuales},
            updated_at = NOW(), version = version + 1
        WHERE id = #{id} AND deleted = FALSE
    </update>

    <update id="softDelete">
        UPDATE <include refid="table"/> SET deleted = TRUE, updated_at = NOW(), version = version + 1 WHERE id = #{id}
    </update>

    <update id="incrementUsos">
        UPDATE <include refid="table"/>
        SET usos_actuales = usos_actuales + 1, updated_at = NOW(), version = version + 1
        WHERE id = #{id} AND deleted = FALSE AND (limite_usos IS NULL OR usos_actuales &lt; limite_usos)
    </update>
</mapper>
