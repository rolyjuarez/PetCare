<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.repository.DisponibilidadMapper">
    <sql id="baseColumns">
        id, proveedor_id, servicio_id, dia_semana, hora_inicio, hora_fin, activo,
        created_at, updated_at, deleted, version
    </sql>
    <sql id="table">disponibilidad</sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="bo.capital.tec.pet.domain.Disponibilidad">
        INSERT INTO <include refid="table"/> (proveedor_id, servicio_id, dia_semana, hora_inicio, hora_fin, activo)
        VALUES (#{proveedorId}, #{servicioId}, #{diaSemana}, #{horaInicio}, #{horaFin}, #{activo})
    </insert>

    <select id="selectById" resultType="bo.capital.tec.pet.domain.Disponibilidad">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        WHERE id = #{id} AND deleted = FALSE
    </select>

    <select id="selectByProveedorYServicio" resultType="bo.capital.tec.pet.domain.Disponibilidad">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        <where>
            proveedor_id = #{proveedorId} AND deleted = FALSE
            <if test="servicioId != null">AND servicio_id = #{servicioId}</if>
        </where>
        ORDER BY dia_semana ASC, hora_inicio ASC
    </select>

    <select id="selectAll" resultType="bo.capital.tec.pet.domain.Disponibilidad">
        SELECT <include refid="baseColumns"/> FROM <include refid="table"/>
        <where>
            deleted = FALSE
            <if test="proveedorId != null">AND proveedor_id = #{proveedorId}</if>
            <if test="servicioId != null">AND servicio_id = #{servicioId}</if>
        </where>
        ORDER BY dia_semana ASC, hora_inicio ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM <include refid="table"/>
        <where>
            deleted = FALSE
            <if test="proveedorId != null">AND proveedor_id = #{proveedorId}</if>
            <if test="servicioId != null">AND servicio_id = #{servicioId}</if>
        </where>
    </select>

    <update id="update" parameterType="bo.capital.tec.pet.domain.Disponibilidad">
        UPDATE <include refid="table"/>
        SET proveedor_id = #{proveedorId}, servicio_id = #{servicioId},
            dia_semana = #{diaSemana}, hora_inicio = #{horaInicio}, hora_fin = #{horaFin}, activo = #{activo},
            updated_at = NOW(), version = version + 1
        WHERE id = #{id} AND deleted = FALSE
    </update>

    <update id="softDelete">
        UPDATE <include refid="table"/> SET deleted = TRUE, updated_at = NOW(), version = version + 1 WHERE id = #{id}
    </update>

    <update id="deleteByProveedorId">
        UPDATE <include refid="table"/> SET deleted = TRUE, updated_at = NOW(), version = version + 1
        WHERE proveedor_id = #{proveedorId} AND deleted = FALSE
    </update>
</mapper>
