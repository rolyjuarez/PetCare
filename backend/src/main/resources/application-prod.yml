spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:petcaredb}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123qwe}
  cache:
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=30m
logging:
  level:
    bo.capital.tec.pet: INFO
    org.springframework: WARN
