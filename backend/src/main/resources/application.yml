server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/petcaredb
    username: postgres
    password: 123qwe
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: America/La_Paz
    serialization:
      write-dates-as-timestamps: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: roly.juarez@gmail.com
    password: srek cder xteq iqnj
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterWrite=10m

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: bo.capital.tec.pet.modules.*.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

jwt:
  secret: ${JWT_SECRET:PETCareSecretKeyForJWTTokenGeneration2024VeryLongAndSecureKeyMustBe256Bits}
  expiration: 3600000
  refresh-expiration: 604800000

app:
  mail:
    from: roly.juarez@gmail.com
  microservices:
    reservation: ${RESERVATION_SERVICE_URL:http://localhost:8081}
    provider: ${PROVIDER_SERVICE_URL:http://localhost:8082}
    payment: ${PAYMENT_SERVICE_URL:http://localhost:8083}

logging:
  level:
    bo.capital.tec.pet: DEBUG
    org.springframework.security: INFO

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
