<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.modules.pago.mapper.PagoCatalogMapper">

    <select id="selectReservaInfo" resultType="bo.capital.tec.pet.modules.pago.dto.ReservaInfoDTO">
        SELECT r.id AS reservaId, r.codigo, r.cliente_id AS clienteId, r.proveedor_id AS proveedorId,
               r.servicio_id AS servicioId, r.modalidad_entrega AS modalidadEntrega,
               r.precio_total AS precioTotal, r.fecha_inicio AS fechaInicio, r.hora_inicio AS horaInicio
        FROM reserva r
        WHERE r.id = #{reservaId} AND r.deleted = FALSE
    </select>

</mapper>
