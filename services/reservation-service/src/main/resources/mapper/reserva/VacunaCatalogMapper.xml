<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.modules.reserva.mapper.VacunaCatalogMapper">

    <select id="selectRequiereCertificado" resultType="boolean">
        SELECT COALESCE(ps.requiere_certificado, FALSE)
        FROM proveedor_servicio ps
        WHERE ps.proveedor_id = #{proveedorId}
          AND ps.id = #{servicioId}
          AND ps.deleted = FALSE
        LIMIT 1
    </select>

    <select id="selectRegistroVacunacion" resultType="bo.capital.tec.pet.modules.reserva.dto.RegistroVacunacionInfoDTO">
        SELECT rv.id,
               rv.mascota_id,
               rv.vacuna_id,
               v.nombre AS vacuna_nombre,
               rv.fecha_aplicacion,
               rv.certificado_url
        FROM registro_vacunacion rv
        LEFT JOIN vacuna v ON v.id = rv.vacuna_id
        WHERE rv.id = #{id} AND rv.deleted = FALSE
    </select>
</mapper>
