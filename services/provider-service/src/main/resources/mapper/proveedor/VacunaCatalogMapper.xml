<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.capital.tec.pet.modules.proveedor.mapper.VacunaCatalogMapper">

    <select id="selectRegistroVacunacion" resultType="bo.capital.tec.pet.modules.proveedor.dto.VacunaInfoDTO">
        SELECT rv.id, rv.mascota_id AS mascotaId, rv.vacuna_id AS vacunaId,
               v.nombre AS vacunaNombre, rv.fecha_aplicacion AS fechaAplicacion,
               rv.certificado_url AS certificadoUrl
        FROM registro_vacunacion rv
        LEFT JOIN vacuna v ON v.id = rv.vacuna_id
        WHERE rv.id = #{id} AND rv.deleted = FALSE
    </select>
</mapper>
